# 2026-05-13 工作记录

## 完成的主要工作

### 1. 实现小程序登录功能
- 创建 `utils/user.js`：用户状态管理单例，正确处理 `token`, `username`, `user_display_name` 等字段
- 创建 `utils/request.js`：统一网络请求工具，封装 `wx.request`，自动注入 Token
- 创建 `pages/login/`：登录页面（用户名+密码），风格与小程序一致
- 修改 `pages/index/`：右上角添加登录按钮和用户信息显示
- 更新 `app.json`：添加登录页面路由
- 创建 `LOGIN_SETUP.md`：配置指南

### 2. 配置生产域名
- 根据 Web 项目 `index.html` 找到生产域名：`https://boheyuan.cn`
- 更新 `utils/request.js` 中的 `BASE_URL` 为生产域名

### 3. 修复问题
- 修复 `utils/user.js`：正确处理接口返回的 `routers`, `permissions`, `menus` 字段（不存储到 userInfo）
- 修复 `pages/index/index.js`：优先显示 `user_display_name`
- 发现 `utils/request.js` 中可能有 `encodeURIComponent` 拼写错误（待修复）

## 关键信息

- **生产域名**：`https://boheyuan.cn`
- **登录接口**：`POST https://boheyuan.cn/api/rbac/login/`
- **接口返回结构**：`{ code: 0, data: { token, username, user_display_name, ... } }`
- **微信小程序要求**：所有请求域名必须在微信公众平台配置为合法域名（仅支持 HTTPS）

## 待办事项

- [ ] 在微信公众平台配置合法域名：`https://boheyuan.cn`
- [ ] 测试登录流程
- [ ] 修复 `utils/request.js` 中的 `encodeURIComponent` 拼写错误（如果存在）
